//(handles operations on user's recipe lists)
import { Directive } from '@angular/core';

@Directive({
  selector: '[appUserlist]',
  standalone: true
})
export class UserlistDirective {

  constructor() { }

}
